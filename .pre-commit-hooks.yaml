- id: baseline
  name: "MoJ secret scanner"
  description: "Scans staged and unstaged changes for hardcoded secrets using gitleaks."
  entry: scripts/gitleaks.sh
  language: script
  pass_filenames: false
  always_run: true
  verbose: true
  stages:
    - pre-commit
